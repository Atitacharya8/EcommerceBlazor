@page "/learnBlazor/parentcomponent"

<h3>Parent Child Relation</h3>

<_ChildComponent OnButtonClick="ShowMessage" Title="this title is passed from Parent Component and displayed." MessageForGrandChild="This message if from you grandparent (ParentComponent) ">
    <ChildContent>my name is render fragment from child component.</ChildContent>
    <DangerChildContent>Danger content from render fragment! </DangerChildContent>

</_ChildComponent>
<p>@messageText</p>

<_AnotherChildComponent placeholder="Placeholder from parent"></_AnotherChildComponent>

@* <_ChildComponent OnButtonClick="ShowMessage" Title="this is second child component.">
</_ChildComponent> *@

@code {
    public string messageText = "";

    private void ShowMessage()
    {
        messageText = "Button clicked from Child component";
    }

  

}
